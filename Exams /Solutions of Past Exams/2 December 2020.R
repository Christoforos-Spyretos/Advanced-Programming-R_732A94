# 2 December 2020

################################################################################

# Problem 2

create_lawnmover <- function(nm, current_location){
  stopifnot( is.numeric(nm), n %% 1 == 0, m %% 1 == 0, n > 1, m > 1,
             is.numeric(current_location), i > 1, i < n, j > 1, j < m)
  
  for ( i in 1:n){
    for ( j in 1:m){
      mowed <- 
      unmowed <-
    }
  }
}